services:
  - type: web
    name: risk-multiplayer-game
    runtime: node
    plan: free
    buildCommand: npm install && cd multiplayer && npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /
